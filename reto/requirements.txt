# Core API deps
fastapi>=0.111,<1
uvicorn[standard]>=0.23,<1
pydantic>=2,<3

# Optional extras used by this repo
agentpy>=0.1.7; python_version < "3.13"
# pillow for future grid mask support (optional)
Pillow>=10,<11
